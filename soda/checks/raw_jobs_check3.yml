filter jobs [today]:
  where: scraped_at::date = CURRENT_DATE

checks for jobs [today]:
  ## ĐOẠN NÀY ĐỂ KIỂM TRA XEM CÓ SPIDERS NÀO KHÔNG THU THẬP ĐƯỢC DỮ LIỆU KHÔNG

  - every_spider_has_data = 10:
      every_spider_has_data query: |
        SELECT COUNT(DISTINCT source_site) FROM jobs
